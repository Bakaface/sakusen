{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"${SORTIE_DONE_DIR:-}\" ] && mkdir -p \"$SORTIE_DONE_DIR\" && tmp=\"$SORTIE_DONE_DIR/.$$.$(date +%s%N).tmp\" && cat > \"$tmp\" && mv \"$tmp\" \"$SORTIE_DONE_DIR/${SORTIE_DONE_PREFIX:-step}-$(date +%s%N).json\" || true"
          }
        ]
      }
    ]
  }
}
